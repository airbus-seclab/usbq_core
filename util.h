#ifndef __UTIL_H
#define __UTIL_H

#include <linux/types.h>
#include <linux/usb/ch9.h>



/* typedef struct buf_t { */
/*    size_t size; */
/*    size_t allocated_size; */
/*    unsigned char *buf; */
/* } buf_t; */

/* int alloc_buf(buf_t *, size_t); */
/* int realloc_buf(buf_t *buf, size_t size); */
/* int bufcpy(buf_t *,void *,size_t,size_t); */
/* void free_buf(buf_t *); */

#endif
